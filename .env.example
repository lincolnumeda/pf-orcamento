# Front-end (Vite expõe no bundle — só a anon key, nunca a service role)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Só para scripts/seed.ts (Node, nunca vai pro bundle do front)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
